{
    "normal": "simple",
    "interpolate": "{{=interpolate}}",
    "interpolateDefault": "{{=keyDefault}}",
    "interpolateDefaultEN": "{{=keyDefaultEN}}",
    "evaluate": "{{for(var i = 0, max = 1; i < max; i += 1) {}}to{{}}}",
    "escape": "{{-escape}}",
    "spaced key": "key",
    "namespace": {
        "in": "We're in {{-escape}}"
    }
}
